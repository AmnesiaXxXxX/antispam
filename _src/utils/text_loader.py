from typing import Dict
import json
import os

_texts: Dict[str, str] = {
    "start": """
<b>👋 Приветствую!</b>

Я антиспам-бот, который помогает поддерживать чистоту в чатах. 🛡️

Чтобы узнать больше о моих возможностях, используйте команду /info.
""",
    "info": """
<b>ℹ️ Информация о боте</b>

🤖 Я автоматически проверяю сообщения на наличие спама и нежелательного контента.

Основные функции:
• Фильтрация спама и рекламы
• Блокировка нежелательных ссылок
• Защита от флуда
• Модерация нецензурной лексики

👮‍♂️ Администраторы могут использовать следующие команды:
/ban - Заблокировать пользователя
/add_badword - Добавить запрещенное слово

📝 По всем вопросам: /contact
""",
}


def load_text(key: str, lang: str = "ru") -> str:
    """
    Загружает текст по ключу для указанного языка.
    По умолчанию используется русский язык.
    """
    return _texts.get(key, f"Text not found: {key}")


def load_custom_texts(file_path: str) -> None:
    """
    Загружает пользовательские тексты из JSON файла.
    """
    if os.path.exists(file_path):
        with open(file_path, "r", encoding="utf-8") as f:
            custom_texts = json.load(f)
            _texts.update(custom_texts)
